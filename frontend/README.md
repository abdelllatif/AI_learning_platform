# Folio Frontend (minimal)

This is a minimal Vite + React scaffold for the Folio app. It provides a simple Nav, Sidebar, and page routes to integrate with the Django backend.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

The dev server proxies `/api` and `/auth` to `http://localhost:8000` by default.
