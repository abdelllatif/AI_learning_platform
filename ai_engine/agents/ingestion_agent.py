from documents.models import Document


def ingest_document(document: Document) -> None:
    """Placeholder ingestion for V1.

    In a full pipeline, this would parse the PDF, perform OCR when needed,
    clean text, chunk content, create embeddings, and persist vectors.
    """
    # TODO: implement PDF parsing, OCR, cleaning, chunking, embeddings
    return
